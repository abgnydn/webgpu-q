node_modules/
dist/
.DS_Store
*.log
# LaTeX build byproducts (paper/ keeps only .tex, .bib, .pdf, .py)
*.aux
*.blg
*.out
.vite/
.wrangler/
.vercel

# Playwright artifacts (regenerated on every run; videos + traces are huge)
e2e/.artifacts/
e2e/.report/
playwright-report/
test-results/

# Local env
.env
.env.local
.env.*.local

# Editor / OS scratch
.idea/
.vscode/
*.swp
*~

# Vitest / coverage
coverage/
*.tsbuildinfo

# Local Claude/agent scratch
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Throwaway benchmark/measurement scratch tests (never committed; deleted after use)
**/_zzz-*.test.ts
